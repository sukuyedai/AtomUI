using AtomUI.Theme.TokenSystem;

namespace AtomUI.Controls;

[ControlDesignToken]
public class StepsToken : AbstractControlDesignToken
{
    public const string ID = "Steps";

    public StepsToken()
        : base(ID)
    {
    }

}